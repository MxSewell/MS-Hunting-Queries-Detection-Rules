// Plug & Play desired IP Address
EntraIdSignInEvents
| where Timestamp >= ago(7d)
| where IPAddress contains "23.94.232.197"
| project Timestamp, Application, AccountUpn, UserAgent, IPAddress, City, State